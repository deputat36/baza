# Манифест импорта

## Назначение

Файл фиксирует порядок переноса данных из репозитория в рабочую Google Таблицу.

Для фактического импорта дополнительно используется генерируемый план:

```text
build/import-plan.md
build/import-plan.csv
```

Он создаётся командой:

```bash
make import-plan
```

## Базовый порядок

1. Сначала импортировать справочники из `data/dictionaries`.
2. Затем импортировать шаблоны из `templates`.
3. После этого переносить черновики из `data/drafts`.
4. В конце настроить фильтры, статусы и выпадающие списки.

## Порядок по XLSX-конфигу

Точный порядок листов и CSV-источников берётся из `scripts/tools/workbook_config.py`.

Перед импортом нужно запустить:

```bash
make preflight
```

После этого открыть:

```text
build/preflight-summary.md
build/import-plan.md
```

## Минимальный набор для первого теста

Для первого теста достаточно перенести:

- ситуации по недвижимости;
- документы по сделкам;
- государственные органы;
- банки;
- FAQ;
- поисковые синонимы;
- частые ошибки.

## Расширенный набор

После первого теста можно добавить:

- нотариусов;
- кадастровых инженеров;
- оценщиков;
- ЖКХ;
- управляющие компании;
- подрядчиков;
- новостройки;
- обучающие чек-листы;
- пользовательские сценарии.

## После импорта

Нужно проверить:

- уникальность ID;
- статусы;
- категории;
- связанные записи;
- отсутствие приватных данных;
- удобство поиска.

## Главное правило

GitHub хранит структуру и безопасные черновики. Рабочая таблица хранит проверенные данные и реальные контакты в закрытом доступе.
